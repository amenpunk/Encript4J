apply plugin : 'java'
apply plugin : 'application'


repositories {
    mavenCentral()
    flatDir {
        dirs 'libs'
    }
}

java {
    toolchain {
        languageVersion.set(JavaLanguageVersion.of(8))
    }
}

dependencies {
}

mainClassName = 'Main'

